(function (angular) {
    'use strict';

    var app= angular.module('myapp');
         app .config(Constructor);
    Constructor.$inject = ['$httpProvider'];
    function Constructor($httpProvider)
    {
        
    }

   



})(window.angular);